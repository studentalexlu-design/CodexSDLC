## doctor 看什麼

- 工作流版本、升級基準線
- `sdlc.config.json` 改了有沒有套用（沒套用的話流程用的是舊值，而畫面上看不出來）
- `guidelines/` 的規範檔有沒有 agent 在讀、`rules.json` 驗不驗得過
- 審核修正輪上限實際生效的是幾輪
- Codex 有沒有信任這個專案的 hooks

結果寫在輸出面板。平常不用手動跑：狀態列左下角的 **SDLC** 會在你改檔時自動更新，點它就是選單。
